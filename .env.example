# Privacy RPC Layer Environment Variables

# Required: Solana RPC endpoint URL
# For local development:
QUICKNODE_RPC_URL=http://localhost:8899

# For QuickNode (replace with your endpoint):
# QUICKNODE_RPC_URL=https://your-endpoint.quiknode.pro/your-api-key/

# Optional: Server port (default: 3000)
PORT=3000

# Optional: Rust log level (default: info)
# Options: error, warn, info, debug, trace
RUST_LOG=privacy_rpc_proxy=debug
